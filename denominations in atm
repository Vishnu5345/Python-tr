def result(deno,n):
    deno.sort()
    deno=deno[::-1]
    for coin in deno:
        while n>=coin:
            print(coin,end=" ")
            n-=coin
            

deno=[1,2,5,10,20,50,100,200,500,10]
n=int(input("Enter any amount:"))
result(deno,n)
